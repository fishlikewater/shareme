## ADDED Requirements

### Requirement: 系统必须以接收端提交确认驱动极速发送进度

系统 MUST 在极速会话准备阶段建立接收端提交确认约束，包括可用在途字节窗口、提交确认超时和块确认语义。发送端 MUST 仅在收到接收端对已发送块的成功提交确认后，才能释放对应在途窗口并推进后续发送。系统 MUST 不得把“数据写入 socket 成功”视为“接收端已经完成落盘”。

#### Scenario: 接收端变慢时发送端主动收敛而非直接失败
- **WHEN** 接收端写盘变慢，但仍能在提交确认超时内持续返回块确认
- **THEN** 发送端必须因在途窗口收紧而放缓发送节拍或降低并发，并继续保持同一极速传输会话而不是提前失败

#### Scenario: 未收到提交确认时不得完成极速会话
- **WHEN** 仍存在未被接收端确认提交的块，或任一块的提交确认超时
- **THEN** 系统不得发送极速完成请求，并必须将该传输标记为失败或进入既有回退路径

## MODIFIED Requirements

### Requirement: 系统必须支持自适应 striping 以提升局域网大文件吞吐

系统 MUST 从单连接开始发送大文件，并根据实时吞吐、发送阻塞、接收端提交延迟和在途窗口压力在离散档位 `1 / 2 / 4 / 8` 之间调整并发连接数。系统 MUST 不得将固定并发度写死为单一值。系统 MUST 仅在接收端提交确认稳定、在途窗口未饱和且升档能够带来有效吞吐收益时提升并发档位。

#### Scenario: 吞吐提升且接收确认稳定时升档
- **WHEN** 当前观察窗口内吞吐持续增长，发送端未出现明显阻塞，且接收端提交确认延迟保持在安全窗口内
- **THEN** 系统必须将会话并发提升到下一离散档位

#### Scenario: 接收确认积压或升档收益不足时降档
- **WHEN** 升档后吞吐未提升、发送阻塞显著增加，或接收端提交确认延迟与在途窗口占用持续恶化
- **THEN** 系统必须将会话并发降回上一个稳定档位
