# 前端 Hook 规范

## 原则

- 仅在逻辑跨组件复用或副作用复杂到影响可读性时抽 Hook。
- 订阅、定时器、事件流必须有清理路径。
- 依赖列表必须真实，不能为压制测试或 lint 隐藏依赖。

## 当前项目注意点

- agent event stream 与 Wails event bridge 都属于长连接/订阅类副作用，封装时必须暴露关闭或重连语义。
- 文件选择、极速发送、普通上传存在入口差异，Hook 不应绕开 `LocalApi`。
- 测试中应验证卸载后的订阅清理，避免重复事件写入 UI。
