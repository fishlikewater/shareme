## 1. 本地文件桥接与入口

- [x] 1.1 新增 `backend/internal/localfile` 模块，封装 Windows 原生文件选择与 `LocalFileLease` 管理
- [x] 1.2 在应用层增加本地文件选择与 lease 校验服务接口
- [x] 1.3 在本地 API 增加 `POST /api/local-files/pick`，并限制为本机 loopback 来源可调用
- [x] 1.4 为本地文件桥接补齐单元测试，覆盖成功选择、来源校验失败、lease 过期和文件失效场景

## 2. 高速会话控制面

- [x] 2.1 在协议层定义极速传输能力协商与会话准备消息结构
- [x] 2.2 在应用层增加 `SendAcceleratedFile` 流程，统一创建 `transferId`、`messageId` 和 `sessionId`
- [x] 2.3 在控制面增加高速会话准备、失败终止与回退触发逻辑
- [x] 2.4 为控制面协商补齐单元测试，覆盖可用、不可用和准备失败回退场景

## 3. 高速数据面与完整性校验

- [x] 3.1 新增独立数据端口监听与 `transferToken` 鉴权逻辑
- [x] 3.2 实现发送端单连接高速数据面，支持按 `offset` 读取和发送数据帧
- [x] 3.3 实现接收端临时文件预分配、覆盖位图跟踪与并发 `WriteAt` 落盘
- [x] 3.4 实现最终完整性校验与原子提交逻辑
- [x] 3.5 补齐数据面测试，覆盖合法连接、非法令牌、校验失败和成功提交场景

## 4. Striping 自适应与回退闭环

- [x] 4.1 在高速发送端实现 `1 / 2 / 4 / 8` 离散档位的 striping 自适应控制
- [x] 4.2 增加吞吐、阻塞和落盘反馈指标，用于升档和降档判断
- [x] 4.3 实现高速路径失败时沿用同一 `transferId` 的自动回退逻辑
- [x] 4.4 补齐自适应与回退测试，验证升档、降档、准备失败回退和不可恢复失败终止

## 5. Web UI 与事件语义

- [x] 5.1 在前端 API 客户端增加本地文件选择与极速发送接口
- [x] 5.2 在 Web UI 增加“极速发送大文件”入口与已选择本地文件展示
- [x] 5.3 将极速传输的准备中、传输中、速率、进度、回退和完成状态接入现有事件流
- [x] 5.4 补齐前端测试，覆盖极速入口展示、状态更新和回退提示

## 6. 集成验收与交付保护

- [x] 6.1 增加配置开关或能力开关，确保高速模式可按条件启用和禁用
- [x] 6.2 完成自动化集成验证，并提供已配对 Windows 设备之间的实机验收矩阵
- [x] 6.3 验证高速失败时普通文件路径仍可闭环，且 UI 不产生重复传输记录
- [x] 6.4 更新相关开发文档与测试说明，明确 V1 范围、限制和验收基线
