## 1. Wails 桌面宿主与运行时拆分

- [x] 1.1 引入 Wails 桌面工程与新的桌面主入口，明确桌面窗口作为正式 UI 宿主
- [x] 1.2 将当前后台启动流程拆分为可复用的运行时核心初始化逻辑，保留局域网发现、配对、对等监听与存储能力
- [x] 1.3 移除正式运行路径中对 localhost Web UI server 的依赖，并改为由桌面宿主管理窗口生命周期与运行时生命周期
- [x] 1.4 建立桌面事件桥接，把现有配对、消息、传输与健康状态事件映射到 Wails 事件通道
- [x] 1.5 恢复无窗口 headless 兼容入口，复用同一运行时核心且不重新引入 localhost 浏览器 UI

## 2. 用户主目录布局与安全迁移

- [x] 2.1 实现默认根目录解析，统一使用用户主目录下的 `.message-share` 作为配置与运行数据根目录
- [x] 2.2 新增 `.message-share/config.json` 配置模型与读写逻辑，确保默认生成且不覆盖用户已修改字段
- [x] 2.3 调整身份文件、数据库、日志、临时目录与回退下载目录的路径解析，统一收敛到新的根目录布局
- [x] 2.4 实现旧默认目录探测、一次性迁移与迁移标记逻辑，保证新目录已存在时不覆盖用户现有内容
- [x] 2.5 补齐目录布局与迁移测试，覆盖首次启动、用户修改保留、旧目录迁移和新旧目录并存场景

## 3. 桌面桥接与文件交互改造

- [x] 3.1 使用 Wails 原生能力替换当前本地文件选择实现，并保留 `LocalFileLease` 安全引用模型
- [x] 3.2 将前端 Bootstrap、配对、消息发送、历史分页、传输更新等调用切换到统一的 Wails 客户端适配层
- [x] 3.3 将普通文件发送改造为桌面宿主直读本地文件并交给运行时处理，去掉浏览器上传到本机桌面宿主的重复临时落盘
- [x] 3.4 调整前端事件订阅与状态装配逻辑，使其消费桌面事件通道而不再依赖 loopback HTTP/WS
- [x] 3.5 补齐桌面桥接与文件交互测试，覆盖原生文件选择、命令返回、事件更新和发送失败错误传播

## 4. 三端适配、构建与冗余清理

- [x] 4.1 建立 Windows、macOS、Linux 的桌面开发与发布构建入口，统一接入 Wails 构建主线
- [x] 4.2 补齐三端运行差异处理，包括用户主目录解析、本地文件选择与桌面窗口启动的最小平台适配
- [x] 4.3 清理旧的 embed Web UI 资源接线、localhost-only 前端环境变量入口和不再需要的本地 Web 开发脚本
- [x] 4.4 删除或收拢与新桌面运行时重复的冗余文件、目录和脚本，保持目录结构与模块职责清晰
- [x] 4.5 更新构建与运行文档，明确三端桌面构建方式、默认目录布局与配置文件位置
- [x] 4.6 更新说明文档，明确桌面正式入口与 headless 兼容入口的职责边界与构建方式

## 5. 验证与交付收口

- [x] 5.1 增加后端验证，覆盖桌面宿主生命周期、目录初始化、迁移逻辑与下载回退目录行为
- [x] 5.2 增加前端验证，覆盖桌面客户端适配、无 localhost 依赖启动和事件驱动状态更新
- [x] 5.3 增加桌面 smoke 验证脚本或流程，确保 Windows、macOS、Linux 构建产物都能完成启动与主界面加载
- [x] 5.4 复核并清理遗留依赖、无用文件与过时说明，确保本次变更以桌面正式入口为主线并移除旧 localhost 交付路径
- [x] 5.5 增加 headless 兼容入口的命令层测试与构建验证，确保其可独立编译运行
