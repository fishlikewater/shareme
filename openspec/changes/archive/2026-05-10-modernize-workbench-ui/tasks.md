## 1. 工作台布局骨架

- [x] 1.1 将大 hero 首屏重构为紧凑应用栏、设备 Dock、会话主工作区和状态区域，并保持 Wails 与 localhost 共用同一 React UI。验证：更新相关 Testing Library 断言并运行 `npm test`。
- [x] 1.2 在 `AppShell` 中新增本地 UI 状态以管理设备 Dock 展开/收起，不改变启动快照、API 调用或后端数据。验证：新增折叠状态测试并运行目标前端测试。

## 2. 设备 Dock 与状态判断

- [x] 2.1 将 `DiscoveryPage` / `DeviceList` 调整为可折叠设备 Dock，展开态显示完整设备信息，收起态保留短名、选中态、配对和可达状态。验证：覆盖展开/收起、选中态和空设备状态测试。
- [x] 2.2 强化设备状态的文本与视觉表达，确保状态不只依赖颜色。验证：检查 `aria-pressed`、可访问名称和状态文案断言。

## 3. 会话主工作区

- [x] 3.1 重排 `ChatPane`，把当前设备上下文、消息流、发送器集中到主工作区。验证：原有发送文字、选择普通文件、选择大文件和历史加载测试继续通过。
- [x] 3.2 将未配对和不可达提示调整为主工作区内的任务提示，并保持不可用发送操作被禁用或隐藏。验证：相关场景测试继续通过。
- [x] 3.3 将 `PairCodeDialog` 视觉整合进主工作区信任建立路径，保留开始配对和确认配对行为。验证：配对相关测试继续通过。

## 4. 状态区域与视觉系统

- [x] 4.1 重排 `HealthBanner` 与 `TransferStatusBanner` 的视觉位置，使正常状态摘要化，异常和活跃传输更可见。验证：传输横幅与健康状态测试继续通过。
- [x] 4.2 重写 `styles.css` 的布局、色彩、圆角、阴影、焦点态、hover 和 reduced-motion 规则，形成现代清爽视觉系统。验证：执行 `npm run build`。

## 5. 响应式与验收

- [x] 5.1 检查 375px、768px、1024px、1440px 宽度下的布局，修复横向滚动、文本溢出和按钮挤压。验证：浏览器截图或手工记录。
- [x] 5.2 执行前端完整验证：`npm test` 与 `npm run build`。若影响桌面构建资源，再执行项目约定的相关构建或全量门禁。
