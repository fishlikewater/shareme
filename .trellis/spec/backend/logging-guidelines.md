# 后端日志规范

## 原则

- 日志服务于定位局域网发现、配对、传输、存储和桌面启动问题，不服务于刷屏。
- 不记录私钥、证书私密内容、完整本地文件路径、文件正文、传输正文。
- 高频传输进度不得逐块输出日志；使用遥测与事件节流。

## 建议字段

- `deviceId`、`peerDeviceId`、`transferId`、`messageId`、`pairingId`。
- 操作名、耗时、结果、错误摘要。
- 网络地址只在排障必要时记录，避免扩大暴露面。

## 禁止

- 用日志替代错误返回或状态事件。
- 在单元测试中依赖不稳定日志文本作为业务断言。
