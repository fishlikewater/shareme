## 1. 接收目录统一交付

- [x] 1.1 增加默认下载目录解析器，支持环境变量覆盖、系统下载目录解析与应用目录回退
- [x] 1.2 让普通文件接收与极速文件接收共用统一的最终提交目录与重名避让逻辑
- [x] 1.3 补齐目录解析与文件提交测试，覆盖系统目录可用、系统目录不可用回退和同名文件避让

## 2. 极速传输接收闭环

- [x] 2.1 扩展极速会话准备与数据面协议，增加接收端在途窗口、块提交确认与确认超时语义
- [x] 2.2 在发送端实现基于接收端提交确认的有界流水线，并将并发升降档纳入确认延迟与窗口压力判断
- [x] 2.3 在接收端实现块落盘确认、完成前全量确认校验和超时失败处理
- [x] 2.4 补齐极速传输测试，覆盖“接收端慢但持续确认时成功完成”和“确认超时导致失败或回退”场景

## 3. 消息历史分页数据层与本地 API

- [x] 3.1 为消息存储增加面向会话历史分页的组合索引和游标查询能力
- [x] 3.2 调整 Bootstrap，仅返回每个会话最近 10 条消息以及继续分页所需的历史边界信息
- [x] 3.3 新增会话历史分页本地 API，支持按会话向前加载更早的 10 条消息
- [x] 3.4 补齐消息分页测试，覆盖首屏最近 10 条、游标边界、无更多历史和重复消息去重

## 4. Web UI 历史加载与状态一致性

- [x] 4.1 扩展前端类型与 API 客户端，接入历史分页查询能力
- [x] 4.2 调整聊天面板与页面状态装配，首屏只展示最近 10 条并在上滚接近顶部时触发分页加载
- [x] 4.3 保持实时事件流与已加载历史共存，避免新消息追加或分页结果插入时出现重复和重置
- [x] 4.4 补齐前端测试，覆盖首屏窗口、滚动分页加载、实时消息追加和列表去重

## 5. 交付验收保护

- [x] 5.1 增加集成测试，验证普通文件与极速文件接收完成后都落到默认下载目录
- [x] 5.2 运行后端与前端自动化测试，确认新分页与传输闭环不破坏现有点对点消息与文件发送能力
- [x] 5.3 更新变更相关文档与验收说明，明确默认下载目录策略、极速接收保护语义与历史分页行为
